@model IEnumerable<GameesStore_client.Models.GenreViewModel>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>
@if (User.Identity.Name == "admin")
{
    <button class="btn btn-outline-danger button">
        @Html.ActionLink("Create new game", "Create")
    </button>
}
<div class="row">
    @foreach (var item in Model)
    {
        <div class="col-md-5">
            <div class="card flex-md-row mb-4 box-shadow h-md-250">
                <div class="card-body d-flex flex-column align-items-start">
                    <strong class="d-inline-block mb-2 text-primary">Genre</strong>
                    <h3 class="mb-0">
                        <a class="text-dark" href="#">@Html.DisplayFor(modelItem => item.Name)</a>
                    </h3>
                    <div class="mb-1 text-muted">@DateTime.Now.Year</div>
                    <p class="card-text mb-auto">@Html.DisplayFor(modelItem => item.Description)</p>
                    <div class="cols-md">
                        <div class="row d-flex justify-content-center align-items-center">
                            @if (User.Identity.Name == "admin")
                            {
                                using (Html.BeginForm("Edit", "Genres", new { Id = item.Id }, FormMethod.Post))
                                {
                                    <input type="submit" value="Edit" class="btn btn-edit my-margin" />
                                }
                                using (Html.BeginForm("Details", "Genres", new { Id = item.Id }, FormMethod.Post))
                                {
                                    <input type="submit" value="Details" class="btn btn-details my-margin" />
                                }
                                using (Html.BeginForm("Delete", "Genres", new { Id = item.Id }, FormMethod.Post))
                                {
                                    <input type="submit" value="Delete" class="btn btn-delete my-margin" />
                                }
                            }
                            else
                            {
                                using (Html.BeginForm("Details", "Genres", new { Id = item.Id }, FormMethod.Post))
                                {
                                    <input type="submit" value="Details" class="btn btn-details my-margin" />
                                }
                            }
                        </div>
                    </div>
                </div>
                <img class="card-img-right img-thumbnail img-fluid" data-src="@Html.DisplayFor(modelItem => item.Image)" alt="@item.Name" @*style="width: 180px; height: 180px;"*@  src="@Html.DisplayFor(modelItem => item.Image)" data-holder-rendered="true">
            </div>
        </div>
    }
    <button type="button" class="btn btn-secondary">
        @Html.ActionLink("Back to Games", "../Games/Index")
    </button>
</div>